/*
*
*	This file contains all the required definitions etc, and is included
*	in every .c file in the program.  It should also be the only thing included
*	in any .c file.
* 
 */

#include<stdio.h>     // for basic IO

#include<pcap.h>      // libpcap, for sniffing
#include<pthread.h>   // libpthreads, for threads

#include"puldb.h"     // this is the database structure that is used


// should include all the pre things..

//int spoof(char*,struct puldb*);
//int interface(char*,struct puldb*);

